@model ShoppingStationery.Models.PhieuDeNghiSc

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white d-flex justify-content-center">
                    <div>
                        <h4 class="mb-0 text-white">Tạo phiếu đề nghị sửa chữa</h4>
                    </div>
                </div>
                <div class="card-body">
                    <form asp-action="Create">
                        <div class="form-group">
                            <label asp-for="YkienCsvc" class="control-label">Ý kiến P.CSVC</label>
                            <input asp-for="YkienCsvc" class="form-control" />
                        </div>
                        
                        <div class="form-group">
                            <label asp-for="NgayDeNghi" class="control-label">Ngày đề nghị</label>
                            <input asp-for="NgayDeNghi" class="form-control" />
                        </div>

                        <div class="form-group">
                            <label asp-for="TrangThai" class="control-label">Trạng thái</label>
                            <input asp-for="TrangThai" class="form-control" value="Đang xem xét" readonly />
                        </div>

                        <div class="form-group">
                            <label asp-for="TongSoLoai" class="control-label">Tổng số loại</label>
                            <input asp-for="TongSoLoai" class="form-control" />
                        </div>

                        <div class="form-group d-flex justify-content-center mt-4">
                            <a asp-action="Index" class="btn btn-secondary me-1 ml-1 float-start">Trở về danh sách</a>
                            <input type="submit" value="Thêm mới" class="btn btn-primary text-white" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
